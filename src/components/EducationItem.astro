---
const { date, degree, institution, description } = Astro.props;
---
<article class="flex flex-col gap-4">
    <h3 class="text-xl font-bold text-yellow-400">{degree}</h3>
    <h4 class="text-lg">{institution}</h4>
    <p class="text-gray-600">{date}</p>
    <p>{description}</p>
</article>